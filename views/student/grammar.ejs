<%- include('../partials/header') %>

<main class="game-container">
    <div class="game-header">
        <div class="activity-info">
            <h1><%= activity.Title %></h1>
            <p><%= activity.Description %></p>
        </div>
        <div class="stats-bar">
            <div class="stat-badge xp-badge">
                <span class="icon">⭐</span>
                <span id="xpCounter"><%= studentStats.TotalXP %></span> XP
            </div>
            <div class="stat-badge coins-badge">
                <span class="icon">🪙</span>
                <span id="coinsCounter"><%= studentStats.TotalCoins || 0 %></span>
            </div>
            <div class="stat-badge level-badge">
                <span class="icon">🏆</span> Nvl <span id="levelCounter"><%= studentStats.Level %></span>
            </div>
        </div>
    </div>

    <!-- Cloze Passage Container -->
    <div class="cloze-container" id="clozeContainer">
        <!-- Rendered by JS -->
    </div>

    <div class="action-area" style="text-align: center; margin-top: 2rem;">
        <button id="submitBtn" class="btn-primary" style="display: none; font-size: 1.2rem; padding: 1rem 2rem; border-radius: 50px;">Verificar Respuestas</button>
    </div>
</main>

<script id="questionsData" type="application/json"><%- JSON.stringify(questions || []) %></script>
<script>
    const activityId = <%= activity.ActivityID %>;
    const passageTitle = "<%- passageTitle || '' %>";
    const passageText = <%- JSON.stringify(passage || '') %>;
</script>

<%- include('../partials/footer') %>
