const { executeQuery } = require("./config/database"); executeQuery("SELECT Q.QuestionID, Q.QuestionText, COUNT(O.OptionID) as NumOptions FROM Questions Q LEFT JOIN QuestionOptions O ON Q.QuestionID = O.QuestionID INNER JOIN Activities A ON Q.ActivityID = A.ActivityID WHERE A.ActivityTypeID = 4 GROUP BY Q.QuestionID, Q.QuestionText").then(res => { console.log(res.recordset.slice(0,5)); process.exit(0); });
